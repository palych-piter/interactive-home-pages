<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://localhost:5432/HomeBoardDB</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">Cure1995</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQL9Dialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!--
        "Update" : Update the database schema for tables specified in code
         on startup, meaning new changes
         will be applied and old structure will be in place even if
         some column have been deleted

         "Create" : Create the database schema for tables specified in code
         from scratch every time on startup
         -->

        <property name="hbm2ddl.auto">validate</property>

        <!--<mapping resource="hibernate.xml"/>-->
        <mapping class="core.Message"/>
        <mapping class="core.User"/>


    </session-factory>

</hibernate-configuration>